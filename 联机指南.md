# GTNH 服务器联机指南

> 适用：3 个好朋友通过 Radmin VPN 虚拟局域网，异地一起玩 GT: New Horizons。

---

## 一、服务器现状

- **游戏版本**：GT: New Horizons（GTNH）**2.9.0-beta-1**（beta 测试版）
- **端口**：25565
- **正版验证**：已开启（online-mode=true）——三个人都**必须拥有正版 Minecraft 账号**，用自己账号登录才能进服
- **客户端版本要求**：客户端必须同为 **2.9.0-beta-1** 才能进服，版本不一致会连不上或进服出错
- **联机方式**：Radmin VPN 虚拟局域网（不暴露公网，朋友连的是 Radmin 虚拟 IP）

---

## 二、主机（开服者）步骤

1. **安装 Radmin VPN 免费版**：到官网 https://www.radmin-vpn.com/ 下载安装（安装到每一台要联机的电脑）。
2. **创建网络**：打开 Radmin VPN，点「Create Network / 创建网络」，起一个**网络名**并**设置密码**，创建成功。
3. **把网络名和密码发给两位朋友**（别发到公开场合）。
4. **保持服务器运行**：双击服务器目录里的 `start.bat`，等控制台出现类似 "Done" 的提示后再让大家进服，期间不要关闭窗口。
5. 把**你的 Radmin IP** 也告诉朋友：
   - 怎么看：Radmin VPN 主界面显示的 **IPv4** 地址就是（26.x.x.x 开头）。
   - 注意：你自己的真实 IP / 路由器公网 IP 不需要给任何人。

---

## 三、朋友（加入者）步骤

1. **安装 Radmin VPN**（同官网下载）。
2. **加入网络**：打开后点「Join Network / 加入网络」，输入主机给的**网络名 + 密码**，加入成功。
3. **添加服务器**：打开 Minecraft（2.9.0-beta-1 客户端），「多人游戏」→「添加服务器」：
   - 服务器名称：随便填，例如 `GTNH`
   - 服务器地址：**主机在 Radmin VPN 里的 IP:25565**
     （例如 `你的 Radmin IP` 这样的写法，把「你的 Radmin IP」换成主机报给你的那串数字）
4. 加入时如提示登录，用你自己的**正版账号**登录即可。

---

## 四、主机自己进服

主机自己玩时，服务器地址填：

- `127.0.0.1:25565`（本机回环地址）

---

## 五、常见问题

**1. 朋友连不上，先检查：**

- 双方的 Radmin VPN 都**开着且在线**（界面里能看到自己/对方的 26.x 虚拟 IP）；
- 双方加入了**同一个网络**（网络名完全一致、密码正确）；
- 服务器还在运行、控制台没有报错；
- 可以试着让朋友 ping 一下主机的 Radmin IP（例如 `ping 你的RadminIP`）看通不通。

**2. 防火墙规则（目前状态：尚未添加成功）**

- 添加规则时需要管理员权限，本次执行未成功，规则**还没有**添加上。
- 如果朋友连不上，主机请按下面步骤添加：
  1. 开始菜单搜索 `cmd`，右键 → **以管理员身份运行**；
  2. 粘贴运行这条命令：

     ```
     netsh advfirewall firewall add rule name="GTNH Minecraft Server" dir=in action=allow protocol=TCP localport=25565 profile=private
     ```

  3. 提示「已成功添加规则」即可；可用下面命令确认：

     ```
     netsh advfirewall firewall show rule name="GTNH Minecraft Server"
     ```

- 如果这样加了还连不上，可以把命令里的 `profile=private` 改成 `profile=any` 再添加一次（放行所有网络类型）。

**3. Radmin VPN 实在不行怎么办？**

- 备用方案：**playit.gg**（https://playit.gg，免费内网穿透）。
- 简单说：主机注册 playit.gg 账号并运行它的客户端，把 Minecraft TCP 隧道指向本机的 25565 端口，它会给你一个**公网域名 + 端口**；朋友**不需要装任何软件**，直接在 Minecraft 多人游戏里填这个公网地址就能进服。需要时再单独教详细步骤。

**4. 版本提醒**

- 本服务器是 **2.9.0-beta-1 测试版**，客户端版本不一致进不来；进服前建议各自备份好自己的存档。

---

*生成日期：2026-09-03（服务器端自动生成，仅用于内部联机协助）*
