# GTNH 2.9.0-beta-1 客户端分发说明

> 目的：把「主机这台电脑上能正常玩的 GTNH 2.9.0-beta-1 客户端」复制给两位朋友。
> 要求：**三人的客户端必须完全一致**（同样的 mods 与 config），否则联机时会出现进服失败、方块/物品错乱等问题。
> 适用人群：有基本电脑操作知识的朋友。
> 联机方式（Radmin VPN、服务器地址怎么填）见同目录下的《联机指南.md》，本文件只讲**客户端怎么装**。

---

## 〇、先搞清楚：这份客户端里哪些是「个人数据」

这台机器上的实例位于（仅打包者需要看）：

```
D:/Games/PrismLauncher 9.4 （需要微软登陆）.7z/PrismLauncher 9.4 （需要微软登陆）/
   PrismLauncher 9.4 （需要微软登陆）/instances/GT_New_Horizons_2.9.0-beta-1_Java_17-25
```

它是标准的 Prism Launcher 实例（根目录有 `instance.cfg`、`mmc-pack.json`、`patches/`、`libraries/`，
游戏本体在 `.minecraft/` 里），**可以直接导出成 Prism 专用 zip 让别人导入**。

体积实测（本机评估，2026-09-03）：

| 位置 | 大小 | 说明 |
|---|---|---|
| 实例整体 | ≈ 4.9 GB | 实例文件夹全部内容 |
| ├ .minecraft/backups | ≈ 3.8 GB | **单人游戏世界自动备份 zip（12 个）——最大的个人数据** |
| ├ .minecraft/mods | ≈ 527 MB | 242 个 mod jar + 若干 mod 子目录，**必须分发** |
| ├ .minecraft/config | ≈ 335 MB | 游戏配置，**必须分发**（与 mods 一起保证三方一致） |
| ├ .minecraft/saves | ≈ 27 MB | `saves/llxll` 单人世界 + `saves/NEI`，**个人数据** |
| ├ .minecraft/resourcepacks | ≈ 52 MB | 用户加装的材质包（分发后三方画面一致） |
| ├ .minecraft/coretweaks 等 | ≈ 60 MB | 官方实例自带的 tweak / 字体等，分发 |
| ├ 日志/崩溃/截图 | ≈ 68 MB | `logs/` 32MB、`screenshots/` 19MB、`crash-reports/` 17MB，个人/垃圾数据 |
| └ 其余零碎 | 几 MB | 见下方「个人数据清单」 |

**个人数据清单**（这些不应该发给朋友，导出前移走；如果自己还想留着，先剪切到别处保存）：

- `.minecraft/backups/` —— 单人世界自动备份，占 3.8 GB（导出前**必须**移走，否则包会大 3.6 GB 以上）
- `.minecraft/saves/llxll`、`.minecraft/saves/NEI` —— 单人存档（想保留就移到别处）
- `.minecraft/journeymap/data`（地图与航点）、`.minecraft/TCNodeTracker/`（灵气节点记录）、
  `.minecraft/visualprospecting/client/`（矿脉探测缓存）
- `.minecraft/options.txt`、`optionsnf.txt`（个人键位/画面设置，可留可删）
- `.minecraft/logs/`、`.minecraft/crash-reports/`、`.minecraft/screenshots/`、
  `hs_err_pid*.log`（崩溃转储）
- `.minecraft/falsepattern/`（fplib 启动时自动生成的依赖，删了也会自己重建，约 26 MB）
- 其他零碎：`usernamecache.json`、`knownkeys.txt`、`.healer.log`、`BotaniaVars.dat`、
  `backups_before_restore/`、`serverutilities/`（服务器数据镜像，朋友端会自动重新生成）

已检查过：`.minecraft` 里**没有**残留任何服务器 IP / 服务器地址记录（saves、options、serverutilities
里都没有），移走上述个人目录后导出即可放心分发。

---

## 方案 A（推荐）：Prism Launcher 导出 zip 分发

### A1. 打包机（主机）步骤

1. **移走个人数据（关键一步）**
   - 关掉游戏。在实例目录 `.minecraft/` 里，把 `backups`、`saves`、`logs`、`crash-reports`、
     `screenshots` 等个人数据**剪切**到别处暂存，例如建一个 `D:\gtnh serve\本机个人数据备份\` 文件夹放进去。
   - 这样导出包里就只剩三人共用的 mods/config/资源包，zip 约 1 GB；不清理直接导会得到 4.6 GB+ 的大包。
2. **打开 Prism Launcher 9.4**，在实例列表里**右键** `GT_New_Horizons_2.9.0-beta-1_Java_17-25` 实例。
3. 选 **「导出实例（Export Instance）」**。
4. 导出格式选 **「导出为 zip」**；名字可填 `GT_New_Horizons_2.9.0-beta-1`（中文名也可以，但建议用英文避免传输/解压兼容问题）。
5. 点导出，选个保存位置（建议存到 `D:\gtnh serve\`）。
6. 得到一个 **zip（预计 0.8～1.0 GB）**。把 zip 发给两位朋友（QQ/网盘/Radmin 传文件均可）。
7. **导完后把第 1 步移走的个人数据剪切回原位**，本机不受任何影响。

> 预计耗时（仅估算，本机实测压缩 mods+config 约 107 秒）：导出这个约 1 GB 的干净包约 **2～5 分钟**；
> 如果没清个人数据直接打整个实例，约 **15～30 分钟**且文件大得多——所以务必先清理。

### A2. 朋友机步骤

1. **安装 Prism Launcher 9.4**
   - 官网下载 Windows 版：https://prismlauncher.org
   - 下载后双击解压/安装（便携版解压到任意文件夹就能用）。
   - 如果官网打不开/下载很慢（被墙），开加速器再下，或让主机把安装包也一起传给你。
2. **导入客户端包**：把收到的 **zip 直接拖进 Prism Launcher 窗口**（也可以：菜单「添加实例（Add Instance）→ 导入本地 zip」），
   等它读完就会出现在实例列表里。**注意：拖进去的必须是 zip（自动导入成功的前提是 zip 里有
   `instance.cfg` 和 `mmc-pack.json`，本方法导出的包两者都有）。**
3. **设置内存（重要）**：右键刚导入的实例 →「编辑实例 → 设置 → Java（内存）」，
   把「最大内存分配」设为 **6144～8192 MB（6～8 GB）**（GTNH 很吃内存，建议 ≥6 GB；
   电脑总内存只有 8 GB 的设 6144 就够，不要设满）。
4. **Java**：这个实例需要 Java 17 以上（主机用的是 Java 25，随 Prism 装在它的 `java/` 目录里，不需要单独装）。
   朋友端如果启动时提示「找不到 Java / 选择 Java 运行时」：
   - Prism 菜单「设置 → Java → 自动检测」，或「下载运行时」选 Microsoft OpenJDK 21 或 25；
   - 之后在「编辑实例 → 设置 → Java」里把 Java 路径/架构指到它即可（以后一般不会再问）。
5. **登录正版微软账号**：Prism 左上角「账户（Accounts）」→ 添加微软账户 → 按网页提示登录自己的正版账号。
   三个人都必须是正版账号（服务器开着正版验证 online-mode）。
6. **启动**：双击实例启动。第一次启动会比较慢（要生成一些缓存），耐心等。
   进游戏后主菜单 → 多人游戏 → 添加服务器，地址按《联机指南.md》里主机给的填（Radmin VPN 里主机的 IP，端口 25565）。

> 三方一致性提醒：导入后**不要**再往 mods 里加任何东西、不要自己删 mod，也不要让客户端自动更新；
> 三人装好后 mod 列表一致即可正常联机。

---

## 方案 B（不用 Prism 导出功能）：直接复制实例文件夹

适用：主机这边不想用「导出」，或朋友**已经装好 Prism** 的情况。本质和方案 A 一样，只是手工搬文件夹。

打包机步骤：

1. 先按「〇」节做同样的个人数据清理（把 `backups`、`saves`、`logs` 等剪切到别处）。
2. 用 7-Zip / WinRAR 把**实例文件夹本身**（即 `GT_New_Horizons_2.9.0-beta-1_Java_17-25` 这个文件夹，
   里面要能看到 `instance.cfg`、`mmc-pack.json`、`.minecraft`）压缩成 **zip 格式**。
   - 注意不要把它外面那层 PrismLauncher 目录也压进去；
   - 用 **zip 格式**（Prism 认 zip；如果压成 7z 则必须先解压，再手动把文件夹放进 Prism 的 instances 目录）。
   - 清理后压缩包约 **0.8～1.0 GB**（不清理则是 4.6 GB+ 且含你的单人存档，不建议）。
3. 把 zip 发给朋友。

朋友机步骤：

- 若已装 Prism 9.4：zip **直接拖进 Prism 窗口**即可自动导入（与方案 A 相同），然后照 A2 的第 3～6 步设置内存并启动。
- 注意：zip 里已包含全部 mods 与配置，朋友端**首次启动不需要联网下载 mod**；网络不通也不影响。

体积与注意事项小结（方案 B）：

| 内容 | 大小 |
|---|---|
| 整个实例（未清理） | ≈ 4.9 GB（压缩后约 4.6 GB+，大部分是压不动的备份 zip） |
| 清理个人数据后的实例 | ≈ 1.0 GB（压缩后约 0.8～1.0 GB） |

---

## 常见问题（FAQ）

- **Q：拖 zip 进 Prism 没反应 / 提示不是有效实例？**
  确认 zip 第一层就是 `instance.cfg`、`mmc-pack.json`、`.minecraft`（方案 A 导出的包没问题；
  方案 B 压缩时不要多包一层外层文件夹）。
- **Q：启动时提示 Java 相关错误？**
  见 A2 第 4 步：设置里「自动检测」或下载 Microsoft OpenJDK 21/25。GTNH 不支持旧 Java，别用 Java 8。
- **Q：内存不够 / 启动很卡 / 闪退？**
  实例设置里内存给 6～8 GB；电脑总内存只有 8 GB 时设 6144 MB，并关掉浏览器等大程序再玩。
  另外确认磁盘剩余空间 ≥ 10 GB（游戏会写大量缓存与日志）。
- **Q：进不了服务器 / 提示版本不一致？**
  先确认三人都装的是这份 2.9.0-beta-1 客户端，且没有人往 mods 里加过东西；
  网络方面按《联机指南.md》检查 Radmin VPN 是否已加入主机的网络（网络名 + 密码）。
- **Q：朋友下载 Prism Launcher 官网很慢？**
  用加速器；或者让主机把 PrismLauncher 整个文件夹（含 java 目录）压缩后直接发给朋友，解压即用。
- **Q：为什么必须三份完全一样？**
  GTNH 会校验/同步大量 mod 与配置；版本或 mod 不一致会导致连不上服务器或进服后各种错乱。

---

## 一句话流程（发给朋友的）

1. 装 Prism Launcher 9.4（prismlauncher.org，打不开就开加速器）。
2. 把我发的 zip 拖进 Prism 窗口。
3. 右键实例 → 编辑实例 → 设置 → 内存调到 6～8 GB。
4. 登录你的正版微软账号。
5. 启动游戏，多人游戏地址按主机给的在 Radmin VPN 里填（见《联机指南.md》）。
