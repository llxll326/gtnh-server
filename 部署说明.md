# GTNH 服务器部署说明（2.9.0-beta-1）

> 面向服务器主人（本机：16 GB 内存 Windows）。面向玩家（Radmin VPN 联机、进服步骤、常见问题）的指引见同目录 [`联机指南.md`](联机指南.md)。

---

## 1. 关键位置

| 项目 | 位置 |
| --- | --- |
| 服务器目录 | `D:\gtnh serve\server\` |
| 启动脚本 | `D:\gtnh serve\server\start.bat`（双击启动） |
| 停止说明 | `D:\gtnh serve\server\stop.txt` |
| 存档目录 | `D:\gtnh serve\server\world\`（**首次启动成功后才会生成**） |
| 服务器配置 | `D:\gtnh serve\server\server.properties` |
| JVM 参数 | `D:\gtnh serve\server\java9args.txt`（勿动） |
| Java（JDK 17） | `C:\Users\mushroom\.devtools\jdk17\jdk-17.0.20.1+1\bin\java.exe` |
| 联机指南 | `D:\gtnh serve\联机指南.md` |

### 为什么用完整 Java 路径、不用"系统 Java"

本机**没有把 Java 装进系统、java 不在 PATH 里**——直接敲 `java` 命令是找不到的。
而 GTNH 2.7+ 的服务端必须用 **Java 17** 运行（lwjgl3ify / RFB 引导的要求），
本机现成可用的就是上表这个 JDK 17。
所以 `start.bat` 里把 Java 的完整路径写死并加了引号，双击即可启动，完全不依赖 PATH。
（若以后想换 Java，只改 `start.bat` 里的这一行路径即可。）

---

## 2. 启动 / 停止 / 判断是否成功

### 启动
双击 `D:\gtnh serve\server\start.bat`，会弹出黑色控制台窗口：
- **首次启动需要 5-10 分钟**（加载 mod + RWG 生成新世界），属正常现象，耐心等待；
- 之后再启动通常 1-3 分钟。

### 判断是否成功
- 控制台出现包含 **`Done (...)!`** 的行，即启动成功；
- 或另开一个 cmd 窗口验证端口在监听：

  ```
  netstat -ano | findstr 25565
  ```

  看到 `TCP  0.0.0.0:25565 ... LISTENING`（或 `127.0.0.1:25565`）即服务已就绪；
- 也可以自己用 2.9.0-beta-1 客户端连 `127.0.0.1:25565`，或让朋友试连。

### 停止
详见 `server\stop.txt`，要点：
- **推荐**：在服务器窗口输入 `stop` 回车，服务器正常保存并退出；
- 不得已再 Ctrl+C；**直接关窗口 = 强杀进程，可能损坏存档，尽量避免**。

---

## 3. 内存分配（固定 6G）

- 启动参数 `-Xms6G -Xmx6G` 表示堆内存最小/最大都固定 **6 GB**（16 GB 机器：还要给系统、本机自开的客户端和其他程序留空间，6G 是稳妥档位）；
- **想调大/调小**：直接编辑 `D:\gtnh serve\server\start.bat`，把 `-Xms6G` 与 `-Xmx6G` 两处一起改成同一数值（如 `-Xms8G -Xmx8G`），保存后重启服务器即生效；
- 这跟 `java9args.txt` **完全无关**——那个文件里是 Java 9+ 的模块/反射类 JVM 参数，**不含内存设置，不要动它**。调内存只改 start.bat。

---

## 4. 已做好的关键配置

| 配置 | 值 | 说明 |
| --- | --- | --- |
| `online-mode` | `true` | **正版验证已开启**，三人必须都有正版账号才能进 |
| `view-distance` | `8` | 视距 8，兼顾体验与性能 |
| `motd` | `GT: New Horizons 2.9.0-beta-1` | 服务器列表里显示的标语 |
| EULA | `eula.txt` 里 `eula=true` | 已同意（否则无法启动） |
| `level-type` | `rwg` | Realistic World Gen（真实世界生成），首次启动按它生成新世界 |

其余现状（均已写入 `server.properties`）：端口 `25565`、难度 3（hard）、`allow-flight=true`、
`spawn-protection=1`、白名单关闭。
改配置后需**重启服务器**才生效；改前最好先备份。

---

## 5. 服务端结构（本服务器是怎么组装的）

- **启动结构 = 官方 2.8.4 服务端结构**：`lwjgl3ify-forgePatches.jar` 引导 + RFB 运行时
  （负责把老版本 Minecraft/Forge 适配到 Java 17 上跑），这是 GTNH 服务端的标准启动方式；
- **mods / config = 2.9.0-beta-1**：从本机 PrismLauncher 的完整 2.9.0-beta-1 客户端实例整份复制；
- **mods 已移除纯客户端 mod**，移入
  `C:\Users\mushroom\AppData\Local\Temp\clientonly\`（即 Git Bash 的 `/tmp/clientonly`）。
  按该目录实际清单共 **19 个**：

  - `custommainmenu` 自定义主菜单
  - `betterloadingscreen` 更好的加载画面
  - `bettercrashes` 崩溃界面美化
  - `gtnhcredits` 启动演职员表
  - `NotEnoughCharacter`（拼音搜索）物品拼音搜索
  - `controlling` 按键冲突管理
  - `defaultkeys` 键位映射
  - `MouseTweaks` 鼠标手势
  - `InGameInfoXML` 游戏内 HUD 信息
  - `Opis` 客户端性能/区块查看
  - `torohealth` 怪物血量显示
  - `overloadedarmorbar` 护甲条显示
  - `duradisplay` 耐久显示
  - `sharewhereiam` 一键分享坐标
  - `freecam` 自由视角
  - `oauth` 微软账号登录
  - `simpleskinbackport` 皮肤显示
  - `nohotbarneeded` 隐藏快捷栏
  - `defaultworldgenerator` 世界类型选择

- **移除原因**：这些是**纯客户端 mod**，代码直接引用客户端独有类
  （`net.minecraft.client.*`、`@SideOnly(Side.CLIENT)` 的渲染/GUI/输入/皮肤等）。
  这类 class 一旦在服务端被加载会抛 `NoClassDefFoundError` 之类异常**直接崩服**；
  而服务端不需要画面、按键、皮肤、HUD，删掉对玩法**零影响**。
  之后若手动往 `server\mods` 添加新 mod，务必先确认该 mod 支持服务端/通用端。

---

## 6. 备份建议（务必养成习惯）

- 存档全部在 `D:\gtnh serve\server\world\` 文件夹里（首次启动成功后生成）；
- **定期**（比如每次玩完）把整个 `world\` 复制到其他位置——移动硬盘/网盘/别的分区均可，**服务器处于停止状态时复制最稳**；
- 在**改 mod、升级/降级版本、改配置、大活动（建筑/刷怪塔/下矿等）之前**先备份一次；
- 恢复：把备份的 `world` 整个覆盖回 `server\world\`，再启动即可。

---

## 7. 危险提醒（beta 测试版，请认真读）

- 本服务器是 **2.9.0-beta-1（beta 测试版）**：带大量新内容，也带大量新 bug，
  **官方不保证稳定性**；
- 可能遇到：随机崩溃、卡顿掉线、mod 报错、区块异常、版本内部不兼容等，都属正常风险；
- **所以最重要的一件事：定期备份存档**（见上节）——测试版 + 不备份 = 进度可能一夜清零；
- 遇到崩溃时，控制台会保留报错；日志在 `server\logs\` 与根目录 `server-console*.log`，可拷出排查/反馈。

---

## 8. 版本一致性要求

- **服务器版本 = 三人的客户端版本，必须完全一致：都是 2.9.0-beta-1**；
- 客户端版本不对会连不上、进服即踢或存档错乱；
- 将来升级服务器版本前，先确认三人客户端全部升级到同一版本，并且**先备份存档**。

---

## 9. 联机方式（Radmin VPN）

服务器在本机跑，三位朋友通过 **Radmin VPN** 虚拟局域网连接。
创建/加入网络、主机查看 Radmin IP、朋友添加服务器、防火墙放行 25565、
以及备用方案 playit.gg——**全部步骤请看：[`联机指南.md`](联机指南.md)**（`D:\gtnh serve\联机指南.md`）。

---

## 附录：为服务端可用化移除的客户端 mod 完整清单（20 个）

注：freecam 因客户端未标记 clientSideOnly，服务端需保留以避免连接被拒绝（已放回）。

| Mod | 原因 |
|---|---|

| InGameInfoXML-2.9.3.jar | 服务端加载即崩（客户端类在服务端被加载） |
| MouseTweaks-2.5.2-GTNH.jar | 服务端加载即崩（客户端类在服务端被加载） |
| Opis-1.4.8-mapless.jar | 服务端加载即崩（客户端类在服务端被加载） |
| bettercrashes-1.4.5-GTNH.jar | 服务端加载即崩（客户端类在服务端被加载） |
| betterloadingscreen-1.7.8-GTNH.jar | 服务端加载即崩（客户端类在服务端被加载） |
| controlling-2.1.7.jar | 服务端加载即崩（客户端类在服务端被加载） |
| coretweaks-0.3.4.6-GTNH.jar | 类转换引致 MinecraftServer.main 丢失（beta 版 coretweaks bug） |
| custommainmenu-1.14.1.jar | 服务端加载即崩（客户端类在服务端被加载） |
| defaultkeys-1.3.1.jar | 服务端加载即崩（客户端类在服务端被加载） |
| defaultworldgenerator-0.4.0.jar | 服务端加载即崩（客户端类在服务端被加载） |
| duradisplay-1.4.2.jar | 服务端加载即崩（客户端类在服务端被加载） |
| gtnhcredits-1.3.1.jar | 服务端加载即崩（客户端类在服务端被加载） |
| nohotbarneeded-0.0.5.jar | 服务端加载即崩（客户端类在服务端被加载） |
| oauth-1.3.4-GTNH.jar | 服务端加载即崩（客户端类在服务端被加载） |
| overloadedarmorbar-1.1.1.jar | 服务端加载即崩（客户端类在服务端被加载） |
| sharewhereiam-2.1.6.jar | 服务端加载即崩（客户端类在服务端被加载） |
| simpleskinbackport-1.0.2-GTNH.jar | 服务端加载即崩（客户端类在服务端被加载） |
| torohealth-1.2.2.jar | 服务端加载即崩（客户端类在服务端被加载） |
| txloader-1.8.11.jar | 提前加载 StringTranslate，破坏 gregtech 早期 mixin |
| 【拼音搜索】NotEnoughCharacter-0.1.4-2.7.0 及以上可用.jar | 服务端加载即崩（客户端类在服务端被加载） |
